# Required for real LLM calls (OpenAI SDK compatible, custom base_url supported)
OPENAI_BASE_URL=https://ai.dianhuomao.shop/v1
OPENAI_API_KEY=
# Example model names may vary by provider; use the exact model string available to your account.
OPENAI_MODEL=
